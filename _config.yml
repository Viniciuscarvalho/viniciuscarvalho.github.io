title: Vinicius Carvalho — iOS Engineer
author: viniciuscarvalho
email: viniciuscarvalhom@icloud.com
description: > # this means to ignore newlines until "show_exerpts:"
  iOS Engineer construindo apps e escrevendo sobre arquitetura, produto e carreira.
show_excerpts: true # set to true to show excerpts on the homepage

# Minima date format
# refer to http://shopify.github.io/liquid/filters/date/ if you want to customize this
sass:
  sass_dir: _sass
plainwhite:
  name: Vinicius Carvalho
  tagline: iOS Engineer · Apps · Arquitetura · Produto
  date_format: "%b %-d, %Y"
  sitemap: true # set to true to generate sitemap.xml content
  search: true # set to true to enable searchbar
  dark_mode: false # site is dark-only now (no toggle)
  html_lang: "pt-br" # set the lang attribute of the <html> tag for the pages.

  # generate social links in footer
  social_links:
    twitter: viniciusc70
    github:  viniciuscarvalho
    linkedIn: in/viniciuscarvalhomarques

    # Mastodon instances
    # mastodon:
    # - username: jekyll
    #   instance: example.com
    # - username: jekyll2
    #   instance: example.com

# If you want to link only specific pages in your header, uncomment
# this and add the path to the pages in order as they should show up
#header_pages:
# - about.md

# Build settings
plugins:
  - jekyll-seo-tag
#  - jekyll-feed

exclude:
  - node_modules
  - package.json
  - postcss.config.js
  - tailwind.config.js
  - assets/css/main.css

# Collections
collections:
  apps:
    output: true
    permalink: /apps/:name/

defaults:
  - scope:
      path: ""
      type: apps
    values:
      layout: app
